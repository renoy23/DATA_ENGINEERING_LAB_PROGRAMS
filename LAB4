import json


def lambda_handler(event, context):

    for record in event['Records']:

        bucket = record['s3']['bucket']['name']

        key = record['s3']['object']['key']

        print("New file received:", bucket, key)


    return {

        'statusCode': 200,

        'body': 'Data processed'

    }
